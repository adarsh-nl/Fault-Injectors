# Tiny smoke-run settings: 2 epochs on the synthetic dataset, CPU-friendly.
epochs: 2
batch_size: 2
num_workers: 0
optimizer: {lr: 1.0e-3, weight_decay: 0.0}
scheduler: {milestones: [1], gamma: 0.5}
amp: false
grad_clip: 10.0
val_every: 1
log_every: 1
train_noise: null

name: cora
voxel_size: [0.4, 0.4]
point_range: [-140.8, -40.0, -3.0, 140.8, 40.0, 1.0]
channels: 256
reg_dim: 7          # B1: OpenCOOD raw-dyaw parity; 8 (sin/cos+atan2) = legacy-loader ablation
cit_strategy: winner_take_all      # winner_take_all | topk | maxout
teacher_enabled: true
pac_enabled: true

"""Command-line entry points.

    train.py      train one model under clean conditions
    evaluate.py   score a checkpoint under exactly one condition
    benchmark.py  the full fault sweep, optionally crossed with bandwidth

``common.py`` is the only module in the package that reads configuration.
"""

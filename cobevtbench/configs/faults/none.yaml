# The reference condition. Every robustness number is a comparison against
# this run, so it must be provably clean: build_bridge(None) creates no
# injector at all rather than injectors configured to do nothing.
name: clean
pipeline: {}
agent_scope: non-ego
sweep: []

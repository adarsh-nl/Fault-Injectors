# Extra dependencies for the information-quality module (src/info_quality).
# The core repo's requirements.txt already provides numpy, matplotlib and tqdm.
# These two are only needed when estimating mutual information.
#
#   pip install -r requirements-info-quality.txt
#
# PyTorch wheels are platform-specific. The line below installs the default
# (CPU/CUDA-autodetected) build from PyPI, which is enough for the estimators.
# For a specific CUDA build, follow https://pytorch.org/get-started/locally/.
torch
scikit-learn

# Keymatch: cora

**Verdict: `NO-CHECKPOINT`**

Generated by `results/diag/render_keymatch.py` from `keymatch_raw.json`, which is produced by `results/diag/keymatch_diag.sbatch`. Read-only measurement: no converter is written and no weights are modified.

## Sources

| side | status | tensors | source |
|---|---|---|---|
| official | `ABSENT` | - | `no released weights exist` |
| reimpl | not dumped — see below | - | `corabench model=cora dataset=opv2v` |

No released weights: CoRA (arXiv:2512.13191v1, AAAI 2026) has no released code
and no released weights. There is nothing to convert; this model's only path to
a number is train-under-the-paper's-protocol, by construction.

### Correction: the reimpl row is not a real failure

`keymatch_raw.json` records this side as `BLOCKED-BY-IMPORT` with

```
AttributeError: module 'corabench.scripts.common' has no attribute 'load'
```

**That is a defect in the diagnostic harness, not in `corabench`.**
`results/diag/keymatch_diag.py` assumes every package exposes the
`load(overrides)` / `build_model(cfg)` pair that `cobevtbench`, `w2cbench` and
`v2xvitbench` share. `corabench` does not: it exposes
`build_model(cfg, grid)` (`corabench/scripts/common.py:166`) and has no `load`
— its entry points compose the config differently.

`corabench` imports and constructs fine; nothing here is evidence otherwise.
The reimpl-side dump was simply not produced.

**It was not re-run**, deliberately: the verdict is `NO-CHECKPOINT`, and with
no official side to compare against, a reimpl key dump answers no question.
Re-running would cost another ~20-minute job (see `docs/debug_log.md` DL-001)
for a file nothing reads. If a `corabench` key dump is ever wanted for another
reason, fix `build_reimpl()` in `keymatch_diag.py` to dispatch on the
package's actual builder signature.

**Verdict rule.** On `pct_official_exact` = share of official tensors matched
by *both* name and shape:

| verdict | rule | meaning |
|---|---|---|
| `CHEAP` | >= 60% | structures correspond; a converter is a rename table |
| `MODERATE` | 15-60% | shapes largely align, module layout differs |
| `NOT-WORTH-IT` | < 15% | different graphs; a "converter" would be a reimplementation |
| `BLOCKED-BY-IMPORT` | reimpl would not construct | measurement impossible, cause recorded |
| `NO-CHECKPOINT` | no released weights exist | nothing to convert |

`pct_official_shape_recoverable_upper_bound` additionally counts official
tensors for which *some* unclaimed reimpl tensor has an identical shape. It
ignores whether such a pairing would be semantically correct, so it is a
strict **upper bound** on what any rename-only converter could ever recover.

